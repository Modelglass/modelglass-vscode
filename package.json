{
  "name": "modelglass-vscode",
  "version": "0.0.0",
  "private": true,
  "description": "Modelglass's VS Code extensions — Cost-Aware Router (packages/router) and the standalone MCP extension (packages/mcp), sharing SecretStorage auth + API config (packages/shared). npm workspaces monorepo; each package publishes independently to the Marketplace.",
  "license": "MIT",
  "workspaces": ["packages/*"],
  "scripts": {
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "build": "npm run build --workspaces --if-present"
  }
}
