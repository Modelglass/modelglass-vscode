{
  "name": "@modelglass/vscode-shared",
  "version": "0.0.0",
  "private": true,
  "description": "Shared SecretStorage auth + API config for the Modelglass VS Code extensions (router, mcp). Not published — a workspace-internal package only.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./config.js": "./src/config.js"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.104.0",
    "typescript": "^5.7.0"
  }
}
